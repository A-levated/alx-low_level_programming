#include "main.h"

/**
 * _isdigit - checks if char is a digit
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	int b = 0;

	if (b >= '0' && b <= '9')
		b = 1;

	return (y);
}
